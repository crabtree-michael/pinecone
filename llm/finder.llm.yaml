finder:
  workflow:
    basic agent workflow:
      defintion: agent-workflow.llm.yaml
      prompt: prompts/finder.md
      initial context getter:
        returns: the contents of the working directory with some depth
        limits:
          depth: 3
          max results per folder: 100
  stand alone cli:
    objective: 
      The finder agent should be able to be able to run as a standalone chat bot for testing.

